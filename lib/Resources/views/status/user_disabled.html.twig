{% block context %}
	{% title 'Email confirmation needed' %}

	<h6>Your account is not yet active</h6>

	<p>An email should have been sent to you at <strong>{{ email }}</strong> when you registered.</p>

	<p>Please click the link in that message to confirm your email address and activate your account.</p>

	<div style="margin: 20px 0px;">
			<form method="POST" action="{{ resend_url }}">
					<input type="hidden" name="email" value="{{ email }}">
					<button type="submit" class="btn btn-default">Resend the message</button>
			</form>
	</div>

	<p>Make sure to check your junk or spam folder. The sender is <strong>{{ from_address }}</strong>.</p>

	<p class="text-muted">
			<a href="{{ path('security.login') }}">Back to Login</a>
	</p>
{% endblock context %}
